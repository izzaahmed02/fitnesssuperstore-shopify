# PR #784 candidate state, head `566b2396` — current record, publish still HOLD

Recorded by: Izza | Date: 2026-09-15 | Candidate: PR #784, head `566b2396cdb5a26ae856c6359323ae025d799834`
Preview: theme `188124725564` (UNPUBLISHED) | Production: theme `186120208700` (MAIN)

Supersedes the head-specific findings in `pr784-prepublish-gate-96013bd-2026-09-10.md`. The head has
moved twice since that record: a merge of main on 15 Sep, then a second merge of main after PR #833
shipped. Yusra's earlier verdicts, the canary record and the deletion pre-flight in
`pr784-prepublish-gate-2026-09-08.md` all still stand and are not restated here.

This is a state-of-the-candidate record, not a verification. The byte comparisons below were
computed from git and from the Admin API. The rendered/visual QA on the one changed file is
outstanding and is called out in section 5.

## 1. The 8 Sep blocker is resolved — the branch no longer trails live work

`pr784-prepublish-gate-2026-09-08.md` section 2 blocked publish because the branch was 17 commits
behind main and publishing would have rolled back #813, #819 and #821 on the live storefront.

The candidate is now level with main on every theme file:

```
git merge-base 566b2396 origin/main            -> 91d51746b
git diff --name-only 91d51746b origin/main     -> docs/qa/pr784-exact-head-delta-qa-2026-09-07.md
                                                  docs/qa/pr784-prepublish-gate-2026-09-08.md
                                                  docs/qa/pr784-prepublish-gate-96013bd-2026-09-10.md
```

The four commits the branch trails by are these QA records only. **Zero theme files.** Publishing
from this head cannot regress any live theme work.

## 2. Byte identity against preview `188124725564` — 24 of 25 exact

Every file in the PR manifest was read back from the preview through the Admin API and compared
against the git blob at `566b2396`. 24 match on both size and MD5.

The one exception is `config/settings_data.json`, and it is expected rather than drift: that file is
theme-editor-owned, Shopify regenerates it per theme, and it has never matched git byte-for-byte on
any theme. The meaningful check is its values, which read correctly in the preview:

| Key | Preview value |
|---|---|
| `ff_hide_permanent_compare_price` | `true` — the guard is armed |
| `sale_promo_ff` | `""` |
| `sale_promo_reman` | `""` |
| `sale_promo_default` | `""` |

## 3. What moved relative to the approved `f0883f0`

22 of the 25 files are byte-identical to the SHA Tim approved. Three differ, none of them the guard:

| File | Why |
|---|---|
| `config/settings_data.json` | campaign copy blanked by the post-sale teardown, #826 |
| `config/settings_schema.json` | blank schema defaults removed, #826 then #845 |
| `snippets/labor-day-sale-note.liquid` | see section 4 |

The guard itself is untouched across every head in this sequence:

```
snippets/ff-permanent-compare-hidden.liquid   2557 bytes   md5 dff1cad18d715c19ff3ea9799f4a08ea
```

identical at `f0883f0`, at `96013bd`, at `566b2396`, and in preview `188124725564`.

## 4. `labor-day-sale-note.liquid` — reconciled with #833, one conflict resolved

PR #833 (September Overstock) merged to main and deployed on 15 Sep. It edits this same file, so the
two were reconciled on this branch.

```
f0883f0 (Yusra QA'd)   7956 bytes   md5 ae40c6c5196d6dea870f33ff2de8aee4
main / live today      8189 bytes   md5 0831ae241e390d912859691212926533
566b2396 (this head)   9550 bytes   md5 7e7dd19861f7833b43beee1949bdb79a
```

Both behaviours are preserved: #833's September card routing, and #784's unavailable-variant guard
from `f0883f0` including the `.campaign-sale-container:empty` stylesheet rule that prevents a blank
gap.

The conflict presented as three closers but was two. `f0883f0` had relocated the base
`{%- endif -%}` from column 0 to inside the card container, so main's first closer and the branch's
are the same tag. Taking both sides verbatim would have left one `endif` too many and shipped
unbalanced Liquid. Tag balance after the resolution is 8 `if` / 8 `endif`, which is the merge base's
6 plus one from each side. CWV Regression passed on this head.

## 5. Outstanding before publish

1. **Yusra's re-verification of `labor-day-sale-note.liquid` at this head.** Its bytes have changed
   since her QA and it now carries two campaigns' logic. The preview already serves this exact file
   (9550 / `7e7dd198`), so it can be checked as-is. Nothing else in the manifest needs re-QA.
2. **Tim's approval of `566b2396`.** His 8 Sep approval covers `f0883f0` only and is explicit that a
   changed head stops the publish.

## 6. Publishing is merging — confirmed, not assumed

The live theme `186120208700` is GitHub-connected to `main`. #833 was merged on 15 Sep and its files
appeared on MAIN without a manual publish step, verified by readback:

| File | MAIN today |
|---|---|
| `snippets/september-overstock-sale-note.liquid` | 3165 / `40ecaf899d0d90333f0d0a2d6f0ac643` |
| `snippets/labor-day-sale-note.liquid` | 8189 / `0831ae241e390d912859691212926533` |
| `sections/header-group.json` | 3640 / `3cf37dc8cc00f744b203fd9104b9236e` |

So merging #784 to main **is** the publish. It must not be merged ahead of Tim's approval.

`snippets/ff-permanent-compare-hidden.liquid` is still absent from MAIN, which confirms the guard is
not yet live and the permanent French Fitness MSRP display is still showing to customers.

## 7. Deletion status

Unchanged and still gated on the publish. Spot-read on 15 Sep: 48 of 50 sampled French Fitness
products still carry a populated `custom.retail_price`. The bounded deletion of the 1,679 backed-up
values has not run.

## 8. What was not changed

No theme published or edited. No product, price, compare-at value, inventory, tag, discount or
metafield definition changed. Repository changes only: the main merges and the conflict resolution
described above, plus this record.
