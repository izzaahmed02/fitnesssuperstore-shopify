# Generated by the harness at run time
img/
case-*.html
shots/
node_modules/
package-lock.json

# The repository root ignores package.json (Shopify theme convention);
# this harness needs its own manifest, so re-include it here.
!package.json

# A run against pages built from another revision (before/after comparisons)
build-info.json
results.other-source.json
