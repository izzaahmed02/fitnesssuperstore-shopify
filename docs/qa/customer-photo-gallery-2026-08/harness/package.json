{
  "name": "customer-photo-gallery-qa-harness",
  "private": true,
  "version": "1.0.0",
  "description": "Reproduces the August 2026 Customer Photo Gallery closeout QA: runs the shipped section's markup, CSS and JS against images generated at the pixel dimensions of the live Shopify assets.",
  "scripts": {
    "setup": "npm install && npx playwright install chromium",
    "fixtures": "node genpng.js",
    "build": "node build.js",
    "qa": "node qa.js",
    "residual": "node residual.js",
    "test": "npm run fixtures && npm run build && npm run qa && npm run residual"
  },
  "devDependencies": {
    "playwright": "1.56.1"
  }
}
