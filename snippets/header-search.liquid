<style>
  .gl-search-fullscreen {
    font-size: 16px !important;
    color: #212529 !important;
    position: fixed !important;
    top: 0 !important;
    left: 0 !important;
    right: 0 !important;
    bottom: 0 !important;
    width: 100% !important;
    z-index: 2147483647 !important;
    overflow: hidden !important;
    background: #f8f9fa !important;
  }

  .header__search, .header__search details {
    width: 100%;
  }

  .search__input::placeholder {
  opacity: 1;
  color: #757575; /* your desired placeholder color */
}

.search__input:focus::placeholder {
  opacity: 1; /* Ensure it's still visible on focus */
  color: #757575;
}


  .search-modal__form {
    max-width: 100% !important;
    width: 100% !important;
    padding: 24px 0px !important;
    display: flex !important;
  }

  .search.search-modal__form{
      padding: 0px !important;
  }

  .predictive-search {
    border: none;
    outline: none;
  }

  .search-modal__content {
    align-items: start !important;
  }

  .field__input,
  .field__label {
    background-color: white !important;
    color: #757575 !important;
    height: 64px;
  }

  .predictive-search__heading {
    color: #212529 !important;
    font-size: 18px !important;
    font-weight: 700 !important;
    letter-spacing: 0 !important;
    text-transform: capitalize !important;
    margin: 0!important;
  }


  .predictive-search__item-query-result mark{
    color: #777;
  }

  .modal__close-button .icon {
    color: black;
  }

  .header predictive-search {
    align-items: center !important;
  }

  .search__button.field__button{
    left: 0!important;
    top: 0!important;
    bottom: 0!important;
    height: auto!important;
    width: 64px;
  }

  .search-modal__content .svg-wrapper svg{
    width: 32px;
    height: 32px;
    fill: #495057;
  }

  .search-modal__content .search__input.field__input{
    padding-right: 9.8rem;
    padding-left: 64px;
    padding-top: 0;
    padding-bottom: 0;
    font-size: 24px;
    font-weight: 400;
    line-height: 64px;
    color: #000 !important;
  }
</style>

<details-modal class="header__search gl-fullscreen-search">
  <details>
    <summary class="customSearch">
      <form action="{{ routes.search_url }}" method="get" role="search" class="search custom-header-search">
        <input
          class="custom-header-search--input"
          id="{{ input_id }}"
          type="search"
          name="q"
          value=""
          placeholder="{{ 'general.search.search' | t }}"
          {%- if settings.predictive_search_enabled -%}
            role="combobox"
            aria-expanded="false"
            aria-owns="predictive-search-results"
            aria-controls="predictive-search-results"
            aria-haspopup="listbox"
            aria-autocomplete="list"
            autocorrect="off"
            autocomplete="off"
            autocapitalize="off"
            spellcheck="false"
          {%- endif -%}
        >
        <input type="hidden" name="options[prefix]" value="last">
        <button
          type="reset"
          class="reset__button field__button{% if search.terms == blank %} hidden{% endif %}"
          aria-label="{{ 'general.search.reset' | t }}"
        >
          <span class="svg-wrapper">
            {{- 'icon-reset.svg' | inline_asset_content -}}
          </span>
        </button>
        {%- if settings.predictive_search_enabled -%}
          <div class="predictive-search predictive-search--header" tabindex="-1" data-predictive-search>
            {%- render 'loading-spinner', class: 'predictive-search__loading-state' -%}
          </div>
          <span class="predictive-search-status visually-hidden" role="status" aria-hidden="true"></span>
        {%- endif -%}
      </form>
    </summary>

    <div
      class="search-modal modal__content gl-search-fullscreen"
      role="dialog"
      aria-modal="true"
      aria-label="{{ 'general.search.search' | t }}"
    >
      <div class="modal-overlay"></div>
      <div class="search-modal__content gl-fullscreen-content" tabindex="-1">
        {%- if settings.predictive_search_enabled -%}
          <predictive-search class="search-modal__form" data-loading-text="{{ 'accessibility.loading' | t }}">
        {%- else -%}
          <search-form class="search-modal__form">
        {%- endif -%}
        <form action="{{ routes.search_url }}" method="get" role="search" class="search search-modal__form">
          <div class="field">
            <div style="width: 100%;">
              <input
                class="search__input field__input"
                id="{{ input_id }}"
                type="search"
                name="q"
                placeholder="I'm looking for..."
                value=""
                {%- if settings.predictive_search_enabled -%}
                  role="combobox"
                  aria-expanded="false"
                  aria-owns="predictive-search-results"
                  aria-controls="predictive-search-results"
                  aria-haspopup="listbox"
                  aria-autocomplete="list"
                  autocorrect="off"
                  autocomplete="off"
                  autocapitalize="off"
                  spellcheck="false"
                {%- endif -%}
              >
            </div>
            <input type="hidden" name="options[prefix]" value="last">
            <button
              type="reset"
              class="reset__button field__button{% if search.terms == blank %} hidden{% endif %}"
              aria-label="{{ 'general.search.reset' | t }}"
            >
              <span class="svg-wrapper">
                {{- 'icon-reset.svg' | inline_asset_content -}}
              </span>
            </button>
            <button class="search__button field__button" aria-label="{{ 'general.search.search' | t }}">
              <span class="svg-wrapper">
                {{- 'icon-search.svg' | inline_asset_content -}}
              </span>
            </button>
          </div>

          {%- if settings.predictive_search_enabled -%}
            <div class="predictive-search predictive-search--header" tabindex="-1" data-predictive-search>
              {%- render 'loading-spinner', class: 'predictive-search__loading-state' -%}
            </div>
            <span class="predictive-search-status visually-hidden" role="status" aria-hidden="true"></span>
          {%- endif -%}
        </form>
        {%- if settings.predictive_search_enabled -%}
          <button
            type="button"
            class="search-modal__close-button modal__close-button link link--text focus-inset"
            aria-label="{{ 'accessibility.close' | t }}"
          >
            <span class="svg-wrapper">
              {{- 'icon-close.svg' | inline_asset_content -}}
            </span>
          </button>
          </predictive-search>
        {%- else -%}
          </search-form>
        {%- endif -%}
      </div>
    </div>
  </details>
</details-modal>

<script>
document.addEventListener('DOMContentLoaded', () => {
  const detailsEl = document.querySelector('.header__search details');

  if (!detailsEl) return;

  const input = detailsEl.querySelector('.search__input');

  detailsEl.addEventListener('toggle', () => {
    if (detailsEl.open) {
      setTimeout(() => {
        input.dispatchEvent(new Event('input', { bubbles: true }));
        input.focus();
      }, 10);
    }
  });
});
</script>