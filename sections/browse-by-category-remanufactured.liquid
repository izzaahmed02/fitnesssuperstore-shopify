{{ 'browse-by-category.css' | asset_url | stylesheet_tag }}
<style>
  .browse-category .category-tabs {
  display:none;
}

.browse-category .tab-header {
  justify-content: center;
  flex-direction: column;
}

.browse-category .tab-header p {
  margin-top: 20px;
  text-align:center;
}


</style>
<div class="browse-by-category">
<div class="tab-header">
  {% if section.settings.heading != blank %}
    <h2>{{ section.settings.heading }}</h2>
  {% endif %}

    {% if section.settings.description != blank %}
    <p>{{ section.settings.description }}</p>
  {% endif %}

  {% assign tab_data = "" %}
  {% assign separator = "" %}
  {% for block in section.blocks %}
    {% if block.settings.tab_handle != blank %}
      {% assign handle = block.settings.tab_handle | handleize %}
      {% unless tab_data contains handle %}
        {% capture tab_data %}{{ tab_data }}{{ separator }}{{ handle }}::{{ block.settings.tab_title }}{% endcapture %}
        {% assign separator = "|" %}
      {% endunless %}
    {% endif %}
  {% endfor %}

  {% assign tab_list = tab_data | split: "|" | compact %}

  <div class="category-tabs">
    {% for tab in tab_list %}
      {% assign parts = tab | split: "::" %}
      {% assign handle = parts[0] %}
      {% assign title = parts[1] %}
      <button type="button" class="{% if forloop.first %}active{% endif %}" data-tab="tab-{{ handle }}">
        {{ title }}
      </button>
    {% endfor %}
  </div>
</div>
  
  {% for tab in tab_list %}
    {% assign parts = tab | split: "::" %}
    {% assign handle = parts[0] %}
    <div class="category-tab-content {% if forloop.first %}active{% endif %}" id="tab-{{ handle }}">
      <div class="slider-tab-{{ handle }}">
        {% for block in section.blocks %}
          {% assign card_handle = block.settings.tab_handle | handleize %}
          {% if card_handle == handle %}
            <div class="category-card">
              <a aria-label="View {{ block.settings.title | default: handle }} collection" href="{{ block.settings.link }}">
                {% if block.settings.image != blank %}
                  <img src="{{ block.settings.image | image_url }}" alt="{{ block.settings.image.alt }}" loading="lazy" decoding="async">
                {% endif %}
                {% if block.settings.title != blank %}
                  <h3>{{ block.settings.title }}</h3>
                {% endif %}
              </a>
            </div>

                     
          {% endif %}
        {% endfor %}
      </div>
    </div>
  {% endfor %}
</div>

<script src="{{ 'browse-cat-remanufactured.js' | asset_url }}" defer="defer"></script>

{% schema %}
{
  "name": "Browse Cat Remanufactured",
  "class": "browse-category",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Section Heading",
      "default": "Browse by Category"
    },
    {
      "type": "text",
      "id": "description",
      "label": "Section Description"
    }
  ],
  "blocks": [
    {
      "type": "card",
      "name": "Category Card",
      "settings": [
        {
          "type": "text",
          "id": "tab_handle",
          "label": "Tab Handle (e.g. cardio)",
          "default": "cardio"
        },
        {
          "type": "text",
          "id": "tab_title",
          "label": "Tab Title",
          "default": "Cardio Equipment"
        },
        {
          "type": "image_picker",
          "id": "image",
          "label": "Card Image"
        },
        {
          "type": "text",
          "id": "title",
          "label": "Card Title"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Link URL"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Browse Cat Remanufactured",
      "category": "Custom"
    }
  ]
}
{% endschema %}