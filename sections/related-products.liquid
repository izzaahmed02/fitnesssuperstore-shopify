{{ 'section-related-products.css' | asset_url | stylesheet_tag }}

{% comment %}
TEST 1: Show metafield raw value
{% endcomment %}
<p style="color:red;">Metafield raw: {{ product.metafields.custom.related_products }}</p>
<p style="color:red;">Metafield .value: {{ product.metafields.custom.related_products.value }}</p>

{% assign related_products_ids = product.metafields.custom.related_products.value %}

{% comment %}
TEST 2: Show metafield size
{% endcomment %}
<p style="color:red;">Metafield IDs count: {{ related_products_ids.size }}</p>

{% if related_products_ids and related_products_ids.size > 0 %}
  <div class="custom-container">
    <div class="featured_products_header">
      {% if section.settings.title != blank %}
        <h2>{{ section.settings.title }}</h2>
      {% endif %}
    </div>

    <div class="featured_products_container">
      {% for id in related_products_ids %}
        {% assign p = all_products[id] %}
        
        {% comment %}
        TEST 3: Show each resolved ID + product title
        {% endcomment %}
        <p style="color:blue;">Resolved ID: {{ id }} → Product: {% if p %}{{ p.title }}{% else %}NOT FOUND{% endif %}</p>
        
        {% if p and p.available %}
          {% render 'related-items', product: p %}
        {% endif %}
      {% endfor %}
    </div>
  </div>

{% else %}
  <p style="color:red;">Metafield empty → showing fallback products</p>

  {% assign fallback_count = 4 %}
  {% assign offset = "now" | date: "%S" | modulo: 10 | times: 1 %}
  {% assign in_stock_products = collections.all.products | where: 'available', true %}
  {% assign products_to_display = in_stock_products | slice: offset, fallback_count %}
  {% if products_to_display.size == 0 %}
    {% assign products_to_display = in_stock_products | slice: 0, fallback_count %}
  {% endif %}

  {% if products_to_display.size > 0 %}
    <div class="custom-container">
      <div class="featured_products_header">
        {% if section.settings.title != blank %}
          <h2>{{ section.settings.title }}</h2>
        {% endif %}
      </div>

      <div class="featured_products_container">
        {% for product in products_to_display %}
          {% comment %}
          TEST 4: Show fallback product titles
          {% endcomment %}
          <p style="color:green;">Fallback product: {{ product.title }}</p>
          
          {% render 'related-items', product: product %}
        {% endfor %}
      </div>
    </div>
  {% endif %}
{% endif %}

<script src="{{ 'related-products.js' | asset_url }}" defer="defer"></script>



{% schema %}
{
  "name": "t:sections.related-products.name",
  "tag": "section",
  "class": "section related-products",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "t:sections.main-list-collections.settings.title.label"
    }
  ]
}
{% endschema %}
