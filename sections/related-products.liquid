{{ 'section-related-products.css' | asset_url | stylesheet_tag }}

<div class="custom-container">
  {{ customer.first_name }}
  <div class="featured_products_header">
    {% if section.settings.title != blank %}
      <h2>{{ section.settings.title }}</h2>
    {% endif %}
  </div>

   <div class="featured_products_container">
  {% assign featured_products = product.metafields.custom.related_products.value %}
  {% for product in featured_products %}
    {% render 'product-items-home', product: product %}
  {% endfor %}
</div>

</div>

<script src="{{ 'related-products.js' | asset_url }}" defer="defer"></script>

{% schema %}
{
  "name": "t:sections.related-products.name",
  "tag": "section",
  "class": "section related-products",
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "t:sections.main-list-collections.settings.title.label"
    }
  ]
}
{% endschema %}
