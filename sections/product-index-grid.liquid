<style>
  #shopify-section-{{ section.id }} {
    padding-top: 80px;
    padding-bottom: 80px;
  }
</style>

<div class="page-width">
  {% for product in collections['body-solid'].products %}
    {{ product.title }}
  {% endfor %}
  {{ collections['body-solid'].title }}
</div>

{% schema %}
  {
    "name": "Product index section",
    "settings": [
      
    ],
    "presets": [
      {
        "name": "Product index section"
      }
    ]
  }
{% endschema %}