{% comment %} Collection banner — inline Read More/Read Less with no fade and smart truncation {% endcomment %}
{{ 'component-collection-hero.css' | asset_url | stylesheet_tag }}

{%- style -%}
.collection-hero__description { position: relative; }
{%- endstyle -%}

<div class="collection-hero{% if section.settings.show_collection_image and collection.image %} collection-hero--with-image{% endif %} color-{{ section.settings.color_scheme }}">
  <div class="collection-hero__inner page-width {% if settings.animations_reveal_on_scroll %} scroll-trigger animate--fade-in{% endif %}">
    <div class="collection-hero__text-wrapper">
      <h1 class="collection-hero__title">{{- collection.title | escape -}}</h1>

      {% if section.settings.show_collection_description and collection.description != blank %}
        <div class="collection-hero__description">
          {% render 'clean-description', description: collection.description %}
        </div>
      {% endif %}

      {% if collection.metafields.custom.subcategories_custom_titles != blank %}
  {% comment %}
    Same-page subcategory filtering (progressive enhancement).
    Each chip keeps a real /collections/<handle> href as a no-JS / crawler fallback.
    When enabled for this collection, assets/subcollection-filter.js intercepts the click
    and applies the subcategory on the SAME page via a URL parameter + History API
    (no reload, no redirect to a separate collection URL).
  {% endcomment %}
  {% assign current_url = collection.url %}
  {% assign ajax_enabled = false %}
  {% if section.settings.enable_subcategory_ajax_filter %}
    {% assign allow_list = section.settings.subcategory_filter_collections | default: '' | downcase %}
    {% if allow_list == blank or allow_list contains collection.handle %}
      {% assign ajax_enabled = true %}
    {% endif %}
  {% endif %}
  <div
    class="collection-hero__related-collections-wrapper"
    data-subcat-filter="{% if ajax_enabled %}true{% else %}false{% endif %}"
    data-filter-key="{{ section.settings.subcategory_filter_key | escape }}"
    data-collection-url="{{ collection.url }}">
    <button class="collection-hero__related-collections-toggle" aria-expanded="false">
      <span>{{ section.settings.related_collections_toggle | default: "Browse subcategories" }}</span>
      <svg class="icon icon-caret" width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
        <path fill-rule="evenodd" clip-rule="evenodd" d="M5.46967 8.46967C5.76256 8.17678 6.23744 8.17678 6.53033 8.46967L12 13.9393L17.4697 8.46967C17.7626 8.17678 18.2374 8.17678 18.5303 8.46967C18.8232 8.76256 18.8232 9.23744 18.5303 9.53033L12.5303 15.5303C12.2374 15.8232 11.7626 15.8232 11.4697 15.5303L5.46967 9.53033C5.17678 9.23744 5.17678 8.76256 5.46967 8.46967Z" fill="#B3B3B3"></path>
      </svg>
    </button>

    <ul class="collection-hero__related-collections">
     {% for subcat in collection.metafields.custom.subcategories_custom_titles.value %}
  {% if subcat.collection != blank %}
    {% assign sub_handle = subcat.collection.value.handle %}
    {% assign sub_title = subcat.custom_title | default: sub_handle %}
    <li>
      <a href="/collections/{{ sub_handle }}"
         class="collection-hero__related-collection {% if request.path contains sub_handle %}active{% endif %}"
         data-filter-value="{{ sub_title | escape }}"
         data-source-url="/collections/{{ sub_handle }}">
        {{ sub_title }}
      </a>
    </li>
  {% endif %}
{% endfor %}

    </ul>
  </div>
  {% if ajax_enabled %}{{ 'subcollection-filter.js' | asset_url | script_tag }}{% endif %}
{% endif %}

      {% if section.metafields.custom.related_collections != blank %}
        <div class="collection-hero__related-collections-wrapper">
          <button class="collection-hero__related-collections-toggle" aria-expanded="false">
            <span>{{ section.settings.related_collections_toggle | default: "Browse subcategories" }}</span>
            <svg class="icon icon-caret" width="24" height="24" viewBox="0 0 24 24" fill="none"
              xmlns="http://www.w3.org/2000/svg"><path fill-rule="evenodd" clip-rule="evenodd"
              d="M5.46967 8.46967C5.76256 8.17678 6.23744 8.17678 6.53033 8.46967L12 13.9393L17.4697 8.46967C17.7626 8.17678 18.2374 8.17678 18.5303 8.46967C18.8232 8.76256 18.8232 9.23744 18.5303 9.53033L12.5303 15.5303C12.2374 15.8232 11.7626 15.8232 11.4697 15.5303L5.46967 9.53033C5.17678 9.23744 5.17678 8.76256 5.46967 8.46967Z"
              fill="#B3B3B3"></path></svg>
          </button>

          <ul class="collection-hero__related-collections">
            {% assign current_url = collection.url %}
            {% for related in collection.metafields.custom.related_collections.value %}
              {% assign active_class = '' %}
              {% if related.url == current_url %}
                {% assign active_class = 'active' %}
              {% endif %}
              <li><a href="{{ related.url }}" class="collection-hero__related-collection {{ active_class }}">{{ related.title }}</a></li>
            {% endfor %}
          </ul>
        </div>
      {% endif %}
    </div>

    {%- if section.settings.show_collection_image and collection.image -%}
      <div class="collection-hero__image-container media">
        <img
          srcset="
            {%- if collection.image.width >= 165 -%}{{ collection.image | image_url: width: 165 }} 165w,{%- endif -%}
            {%- if collection.image.width >= 360 -%}{{ collection.image | image_url: width: 360 }} 360w,{%- endif -%}
            {%- if collection.image.width >= 535 -%}{{ collection.image | image_url: width: 535 }} 535w,{%- endif -%}
            {%- if collection.image.width >= 750 -%}{{ collection.image | image_url: width: 750 }} 750w,{%- endif -%}
            {%- if collection.image.width >= 1070 -%}{{ collection.image | image_url: width: 1070 }} 1070w,{%- endif -%}
            {%- if collection.image.width >= 1500 -%}{{ collection.image | image_url: width: 1500 }} 1500w,{%- endif -%}
            {{ collection.image | image_url }} {{ collection.image.width }}w"
          src="{{ collection.image | image_url: width: 750 }}"
          sizes="(min-width: {{ settings.page_width }}px) {{ settings.page_width | minus: 100 | divided_by: 2 }}px,
                 (min-width: 750px) calc(50vw - 130px),
                 calc(50vw - 55px)"
          alt="{{ collection.image.alt | escape }}"
          width="{{ collection.image.width }}"
          height="{{ collection.image.height }}">
      </div>
    {%- endif -%}
  </div>
</div>

<script>
  document.querySelectorAll('.collection-hero__related-collections-toggle').forEach(function(btn) {
    btn.addEventListener('click', function() {
      var expanded = this.getAttribute('aria-expanded') === 'true';
      this.setAttribute('aria-expanded', String(!expanded));
      var list = this.nextElementSibling;
      if (list) {
        list.classList.toggle('is-open', !expanded);
      }
    });
  });
</script>

{% schema %}
{
  "name": "t:sections.main-collection-banner.name",
  "class": "section collnBanner",
  "settings": [
    { "type": "paragraph", "content": "t:sections.main-collection-banner.settings.paragraph.content" },
    { "type": "checkbox", "id": "show_collection_description", "default": true, "label": "t:sections.main-collection-banner.settings.show_collection_description.label" },
    { "type": "checkbox", "id": "show_collection_image", "default": false, "label": "t:sections.main-collection-banner.settings.show_collection_image.label" },
    { "type": "text", "id": "related_collections_toggle", "label": "t:sections.main-collection-banner.settings.related_collections_toggle.label", "default": "Browse subcategories" },
    { "type": "header", "content": "Subcategory filtering" },
    { "type": "checkbox", "id": "enable_subcategory_ajax_filter", "label": "Apply subcategories as same-page filters (no redirect)", "default": true, "info": "When on, clicking a subcategory filters the current collection page via a URL parameter using the History API instead of opening a separate collection page. Falls back to normal links if JavaScript is unavailable." },
    { "type": "text", "id": "subcategory_filter_collections", "label": "Enable only on these collections", "default": "treadmills", "info": "Comma-separated collection handles (e.g. treadmills). Leave blank to enable on every collection that has subcategories." },
    { "type": "text", "id": "subcategory_filter_key", "label": "Boost filter URL key (optional)", "info": "The Boost Smart Filter parameter key that maps to the subcategory filter (e.g. pf_t_...). Get this from your Boost filter configuration. Leave blank to fetch the subcategory collection's grid directly and show a readable ?subcategory= parameter." },
    { "type": "color_scheme", "id": "color_scheme", "label": "t:sections.all.colors.label", "default": "scheme-1" }
  ]
}
{% endschema %}
