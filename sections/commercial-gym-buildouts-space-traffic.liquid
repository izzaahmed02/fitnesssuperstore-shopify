{{ 'section-multicolumn.css' | asset_url | stylesheet_tag }}
{{ 'component-slider.css' | asset_url | stylesheet_tag }}

{%- style -%}
    .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
    }

    #shopify-section-{{ section.id }} {
    background-color: {{ section.settings.bg }};
    }

    /* Full-width page requirement — override the theme's default contained .page-width
       for this section only (does not affect .page-width elsewhere on the site).
       Figma node 4585:81433 (mobile equivalent) shows a 16px side inset, not 40px — see
       the same fix already applied to commercial-gym-buildouts-facility-types.liquid. */
    #shopify-section-{{ section.id }} .page-width {
    max-width: 100%;
    margin: 0;
    padding-left: 16px;
    padding-right: 16px;
    }

    @media screen and (min-width: 750px) {
    #shopify-section-{{ section.id }} .page-width {
    padding-left: 40px;
    padding-right: 40px;
    }
    }

    #shopify-section-{{ section.id }} .multicolumn-card {
    text-align: {{ section.settings.text_align }};
    }

    /* Figma node 4585:81469 "Heading" — 12px gap between heading and subheading (heading
       50px + gap 12px + subheading 72px = 134px frame height, confirmed exactly). */
    #shopify-section-{{ section.id }} .multicolumn__subheading {
    text-align: center;
    max-width: 800px;
    margin: 12px auto 0;
    }

    /* Figma node 4612:86423/4612:86424 (mobile) — Oswald SemiBold 20px/26px; Lato Regular 14px/20px #4f4f55 */
    #shopify-section-{{ section.id }} .title.h1 {
    font-size: 20px;
    line-height: 26px;
    }

    #shopify-section-{{ section.id }} .multicolumn__subheading {
    font-family: 'Lato', sans-serif;
    font-size: 14px;
    line-height: 20px;
    color: #4f4f55;
    }

    #shopify-section-{{ section.id }} .multicolumn-card__info-title {
    color: #000000;
    }

    #shopify-section-{{ section.id }} .multicolumn-card__info .rte {
    font-family: 'Lato', sans-serif;
    font-size: 16px;
    line-height: 24px;
    color: #393940;
    }

    /* Figma node 4585:81436 "Benefit cards" — title→paragraph gap is 8px, not the
       theme's default 15px title margin-bottom collapsed with the 1rem (16px) sibling
       margin. Zero the title's own margin so the sibling's margin-top is the only source
       of the gap, giving an exact 8px regardless of margin-collapsing. */
    #shopify-section-{{ section.id }} .multicolumn-card__info-title {
    margin-bottom: 0;
    }
    #shopify-section-{{ section.id }} .multicolumn-card__info > :nth-child(2) {
    margin-top: 8px;
    }

    /* Figma node 4585:81436 — 20px gap between the concept-diagram image and the text
       below it, and an 8px corner radius on the image (currently unset/square). */
    #shopify-section-{{ section.id }} .multicolumn-card__image-wrapper + .multicolumn-card__info {
    margin-top: 20px;
    }
    #shopify-section-{{ section.id }} .multicolumn-card__image {
    border-radius: 8px;
    }

    /* Figma node 4585:81470/4585:81471 (desktop) — Oswald SemiBold 38px/50px; Lato Regular 16px/24px #393940 */
    @media screen and (min-width: 750px) {
    #shopify-section-{{ section.id }} .title.h1 {
    font-size: 38px;
    line-height: 50px;
    }
    #shopify-section-{{ section.id }} .multicolumn__subheading {
    font-size: 16px;
    line-height: 24px;
    color: #393940;
    }

    /* Figma node 4585:81433 — 28px gap between the heading block and the cards row.
       Zero the title-wrapper's own margin so the sibling's margin-top is the only
       source of the gap, giving an exact 28px regardless of the theme's default. */
    #shopify-section-{{ section.id }} .multicolumn__title {
    margin-bottom: 0;
    }
    #shopify-section-{{ section.id }} .multicolumn__title + * {
    margin-top: 28px;
    }

    /* Figma node 4585:81435 "benefits" — 12px gap between the 4 cards (4 × 311px card
       + 3 × 12px gap = 1280px, confirmed exactly), not the theme's default 32px. */
    #shopify-section-{{ section.id }} .multicolumn-list.grid {
    gap: 12px;
    }

    /* Figma node 4585:81436 "Benefit cards" — 32px card padding, not the theme's
       default 24px. */
    #shopify-section-{{ section.id }} .multicolumn-list__item {
    padding: 32px;
    }
    }

    /* Figma node 4598:83643 — Lato Italic 14px/20px, #808080; 28px gap from the cards
       row above (was 2rem/32px). */
    #shopify-section-{{ section.id }} .space-traffic__disclaimer {
    text-align: center;
    max-width: 800px;
    margin: 28px auto 0;
    font-family: 'Lato', sans-serif;
    font-style: italic;
    font-size: 14px;
    line-height: 20px;
    color: #808080;
    }

    @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top }}px;
    padding-bottom: {{ section.settings.padding_bottom }}px;
    }
    }

    @media screen and (max-width: 750px) {
    #shopify-section-{{ section.id }} .multicolumn-card {
    text-align: start;
    }
    }
{%- endstyle -%}

{%- liquid
    assign columns_mobile_int = section.settings.columns_mobile | plus: 0
    assign show_mobile_slider = false
    if section.settings.swipe_on_mobile and section.blocks.size > columns_mobile_int
        assign show_mobile_slider = true
    endif
-%}

<div class="multicolumn {% unless section.settings.background_style == 'none' and settings.text_boxes_border_thickness > 0 or settings.text_boxes_shadow_opacity > 0 %} background-{{ section.settings.background_style }}{% endunless %}{% if section.settings.title == blank %} no-heading{% endif %}">
    <div class="page-width multicolumn-padding section-{{ section.id }}-padding isolate{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}"
    {% if settings.animations_reveal_on_scroll %}
        data-cascade
            {% endif %}>
        {%- unless section.settings.title == blank -%}
            <div class="title-wrapper-with-link title-wrapper--self-padded-mobile title-wrapper--no-top-margin multicolumn__title">
                <h2 class="title inline-richtext {{ section.settings.heading_size }}">
                    {{ section.settings.title }}
                </h2>
                {%- unless section.settings.subheading == blank -%}
                    <div class="multicolumn__subheading rte">{{ section.settings.subheading }}</div>
                {%- endunless -%}
                {%- if section.settings.button_label != blank and show_mobile_slider -%}
                    <a href="{{ section.settings.button_link }}" class="link underlined-link large-up-hide">
                        {{- section.settings.button_label | escape -}}
                    </a>
                {%- endif -%}
            </div>
        {%- endunless -%}
        <slider-component class="slider-mobile-gutter">
            <ul
                    class="multicolumn-list contains-content-container grid grid--{{ section.settings.columns_mobile }}-col-tablet-down grid--{{ section.settings.columns_desktop }}-col-desktop{% if show_mobile_slider %} slider slider--tablet grid--peek{% endif %}"
                    id="Slider-{{ section.id }}"
                    role="list">
                {%- liquid
                    assign highest_ratio = 0
                    for block in section.blocks
                        if block.settings.image.aspect_ratio > highest_ratio
                            assign highest_ratio = block.settings.image.aspect_ratio
                        endif
                    endfor
                -%}
                {%- for block in section.blocks -%}
                    {%- assign empty_column = '' -%}
                    {%- if block.settings.image == blank
                    and block.settings.title == blank
                    and block.settings.text == blank
                    -%}
                        {%- assign empty_column = ' multicolumn-list__item--empty' -%}
                    {%- endif -%}
                    <li id="Slide-{{ section.id }}-{{ forloop.index }}"
                        class="multicolumn-list__item grid__item{% if section.settings.swipe_on_mobile %} slider__slide{% endif %}{% if section.settings.column_alignment == 'center' %} center{% endif %}{{ empty_column }}{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--slide-in{% endif %}"
                            {{ block.shopify_attributes }}
                    {% if settings.animations_reveal_on_scroll %}
                        data-cascade
                        style="--animation-order: {{ forloop.index }};"
                            {% endif %}>
                        <div class="multicolumn-card content-container">
                            {%- if block.settings.image != blank -%}
                                {% if section.settings.image_ratio == 'adapt' or section.settings.image_ratio == 'circle' %}
                                    {% assign spaced_image = true %}
                                {% endif %}
                                <div class="multicolumn-card__image-wrapper multicolumn-card__image-wrapper--{{ section.settings.image_width }}-width{% if section.settings.image_width != 'full' or spaced_image %} multicolumn-card-spacing{% endif %}">
                                    <div
                                            class="media media--transparent media--{{ section.settings.image_ratio }}"
                                            {% if section.settings.image_ratio == 'adapt' %}
                                                style="padding-bottom: {{ 1 | divided_by: highest_ratio | times: 100 }}%;"
                                            {% endif %}
                                    >
                                        {%- liquid
                                            assign number_of_columns = section.settings.columns_desktop
                                            assign number_of_columns_mobile = section.settings.columns_mobile
                                            assign grid_space_desktop = number_of_columns | minus: 1 | times: settings.spacing_grid_horizontal | plus: 100 | append: 'px'
                                            assign grid_space_tablet = number_of_columns_mobile | minus: 1 | times: settings.spacing_grid_horizontal | plus: 100 | append: 'px'
                                            assign grid_space_mobile = number_of_columns_mobile | minus: 1 | times: settings.spacing_grid_horizontal | divided_by: 2 | plus: 30 | append: 'px'
                                            if section.settings.image_width == 'half'
                                                assign image_width = 0.5
                                            elsif section.settings.image_width == 'third'
                                                assign image_width = 0.33
                                            else
                                                assign image_width = 1
                                            endif
                                        -%}
                                        {% capture sizes %}
                                            (min-width: {{ settings.page_width }}px) calc(({{ settings.page_width }}px - {{ grid_space_desktop }}) * {{ image_width }} /  {{ number_of_columns }}),
                                            (min-width: 990px) calc((100vw - {{ grid_space_desktop }}) * {{ image_width }} / {{ number_of_columns }}),
                                            (min-width: 750px) calc((100vw - {{ grid_space_tablet }}) * {{ image_width }} / {{ number_of_columns_mobile }}),
                                            calc((100vw - {{ grid_space_mobile }}) * {{ image_width }} / {{ number_of_columns_mobile }})
                                        {% endcapture %}
                                        {{
                                        block.settings.image
                                        | image_url: width: 3200
                                        | image_tag:
                                        widths: '50, 75, 100, 150, 200, 300, 400, 500, 750, 1000, 1250, 1500, 1750, 2000, 2250, 2500, 2750, 3000, 3200',
                                        sizes: sizes,
                                        class: 'multicolumn-card__image'
                                        }}
                                    </div>
                                </div>
                            {%- endif -%}
                            <div class="multicolumn-card__info">
                                {%- if block.settings.title != blank -%}
                                    <p class="multicolumn-card__info-title">{{ block.settings.title }}</p>
                                {%- endif -%}
                                {%- if block.settings.text != blank -%}
                                    <div class="rte">{{ block.settings.text }}</div>
                                {%- endif -%}
                            </div>
                        </div>
                    </li>
                {%- endfor -%}
            </ul>
            {%- if show_mobile_slider -%}
                <div class="slider-buttons large-up-hide">
                    <button
                            type="button"
                            class="slider-button slider-button--prev"
                            name="previous"
                            aria-label="{{ 'general.slider.previous_slide' | t }}"
                    >
                        {% render 'icon-caret' %}
                    </button>
                    <div class="slider-counter caption">
                        <span class="slider-counter--current">1</span>
                        <span aria-hidden="true"> / </span>
                        <span class="visually-hidden">{{ 'general.slider.of' | t }}</span>
                        <span class="slider-counter--total">{{ section.blocks.size }}</span>
                    </div>
                    <button
                            type="button"
                            class="slider-button slider-button--next"
                            name="next"
                            aria-label="{{ 'general.slider.next_slide' | t }}"
                    >
                        {% render 'icon-caret' %}
                    </button>
                </div>
            {%- endif -%}
        </slider-component>
        {%- unless section.settings.disclaimer == blank -%}
            <p class="space-traffic__disclaimer">{{ section.settings.disclaimer }}</p>
        {%- endunless -%}
    </div>
</div>

{% schema %}
{
  "name": "CGB Space & Traffic",
  "class": "section",
  "tag": "section",
  "disabled_on": {
    "groups": [
      "header",
      "footer"
    ]
  },
  "settings": [
    {
      "type": "color",
      "id": "bg",
      "label": "Background color",
      "default": "#fff"
    },
    {
      "type": "inline_richtext",
      "id": "title",
      "default": "Plan by Space and Traffic",
      "label": "Heading"
    },
    {
      "type": "richtext",
      "id": "subheading",
      "default": "<p>Room size is only part of the story. The right equipment mix also depends on who uses the space and how busy it gets. A quiet amenity room and a high-traffic commercial floor of the same size are planned very differently. The starting points below are conceptual planning guidance, not fixed packages.</p>",
      "label": "Subheading"
    },
    {
      "type": "select",
      "id": "heading_size",
      "options": [
        {
          "value": "h2",
          "label": "t:sections.all.heading_size.options__1.label"
        },
        {
          "value": "h1",
          "label": "t:sections.all.heading_size.options__2.label"
        },
        {
          "value": "h0",
          "label": "t:sections.all.heading_size.options__3.label"
        },
        {
          "value": "hxl",
          "label": "t:sections.all.heading_size.options__4.label"
        },
        {
          "value": "hxxl",
          "label": "t:sections.all.heading_size.options__5.label"
        }
      ],
      "default": "h1",
      "label": "t:sections.all.heading_size.label"
    },
    {
      "type": "select",
      "id": "image_width",
      "options": [
        {
          "value": "icon",
          "label": "Icon width"
        },
        {
          "value": "third",
          "label": "t:sections.multicolumn.settings.image_width.options__1.label"
        },
        {
          "value": "half",
          "label": "t:sections.multicolumn.settings.image_width.options__2.label"
        },
        {
          "value": "full",
          "label": "t:sections.multicolumn.settings.image_width.options__3.label"
        }
      ],
      "default": "full",
      "label": "t:sections.multicolumn.settings.image_width.label"
    },
    {
      "type": "select",
      "id": "image_ratio",
      "options": [
        {
          "value": "adapt",
          "label": "t:sections.multicolumn.settings.image_ratio.options__1.label"
        },
        {
          "value": "portrait",
          "label": "t:sections.multicolumn.settings.image_ratio.options__2.label"
        },
        {
          "value": "square",
          "label": "t:sections.multicolumn.settings.image_ratio.options__3.label"
        },
        {
          "value": "circle",
          "label": "t:sections.multicolumn.settings.image_ratio.options__4.label"
        }
      ],
      "default": "square",
      "label": "t:sections.multicolumn.settings.image_ratio.label"
    },
    {
      "type": "range",
      "id": "columns_desktop",
      "min": 1,
      "max": 6,
      "step": 1,
      "default": 4,
      "label": "t:sections.multicolumn.settings.columns_desktop.label"
    },
    {
      "type": "select",
      "id": "column_alignment",
      "options": [
        {
          "value": "left",
          "label": "t:sections.multicolumn.settings.column_alignment.options__1.label"
        },
        {
          "value": "center",
          "label": "t:sections.multicolumn.settings.column_alignment.options__2.label"
        }
      ],
      "default": "left",
      "label": "t:sections.multicolumn.settings.column_alignment.label"
    },
    {
      "type": "select",
      "id": "text_align",
      "options": [
        {
          "label": "Left",
          "value": "left"
        },
        {
          "label": "Center",
          "value": "center"
        },
        {
          "label": "Right",
          "value": "right"
        }
      ],
      "default": "left",
      "label": "Columns text alignment"
    },
    {
      "type": "text",
      "id": "button_label",
      "label": "t:sections.multicolumn.settings.button_label.label"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "t:sections.multicolumn.settings.button_link.label"
    },
    {
      "type": "richtext",
      "id": "disclaimer",
      "default": "<p>Conceptual equipment placement only. Final layouts require on-site verification and, where applicable, review by qualified professionals.</p>",
      "label": "Disclaimer text"
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.all.colors.label",
      "default": "scheme-1"
    },
    {
      "type": "header",
      "content": "t:sections.multicolumn.settings.header_mobile.content"
    },
    {
      "type": "select",
      "id": "columns_mobile",
      "options": [
        {
          "value": "1",
          "label": "t:sections.multicolumn.settings.columns_mobile.options__1.label"
        },
        {
          "value": "2",
          "label": "t:sections.multicolumn.settings.columns_mobile.options__2.label"
        }
      ],
      "default": "1",
      "label": "t:sections.multicolumn.settings.columns_mobile.label"
    },
    {
      "type": "checkbox",
      "id": "swipe_on_mobile",
      "default": false,
      "label": "t:sections.multicolumn.settings.swipe_on_mobile.label"
    },
    {
      "type": "header",
      "content": "t:sections.all.padding.section_padding_heading"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_top",
      "default": 56
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_bottom",
      "default": 56
    }
  ],
  "blocks": [
    {
      "type": "column",
      "name": "t:sections.multicolumn.blocks.column.name",
      "settings": [
        {
          "type": "image_picker",
          "id": "image",
          "label": "Concept layout image"
        },
        {
          "type": "inline_richtext",
          "id": "title",
          "label": "t:sections.multicolumn.blocks.column.settings.title.label"
        },
        {
          "type": "richtext",
          "id": "text",
          "label": "t:sections.multicolumn.blocks.column.settings.text.label"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "CGB Space & Traffic",
      "settings": {
        "columns_desktop": 4,
        "column_alignment": "left",
        "text_align": "left"
      },
      "blocks": [
        {
          "type": "column",
          "settings": {
            "title": "Around 500 sq. ft.",
            "text": "<p>A compact room where every piece has to earn its footprint. Expect a small cardio group, one multi-station strength unit or functional trainer, and a free weight corner with adjustable benches. Flooring and storage matter more here than machine count.</p>"
          }
        },
        {
          "type": "column",
          "settings": {
            "title": "Around 1,000 sq. ft.",
            "text": "<p>Enough room to separate cardio from strength. A wider cardio line, several selectorized machines covering the main movement patterns, a rack or half rack, and a dedicated free weight area with dumbbells across a usable weight range.</p>"
          }
        },
        {
          "type": "column",
          "settings": {
            "title": "Around 1,500 sq. ft.",
            "text": "<p>The point where a room starts to feel like a gym rather than an amenity. Room for a full cardio row, a broader selectorized circuit, plate-loaded pieces, a rack or rig, and an open functional zone that does not compete with the lifting area.</p>"
          }
        },
        {
          "type": "column",
          "settings": {
            "title": "2,500 sq. ft. and above",
            "text": "<p>Planned in zones rather than as one room. Traffic patterns, sightlines and peak-hour flow drive the layout as much as the equipment list. Expect a full cardio bank, distinct selectorized and plate-loaded areas, multiple racks or a rig, and dedicated turf or functional space.</p>"
          }
        }
      ]
    }
  ]
}
{% endschema %}
