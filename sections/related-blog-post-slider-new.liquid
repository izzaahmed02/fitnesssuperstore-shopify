{% comment %}
Titan-style Blog Article Slider - Full Pixel-Perfect Rewrite
- Matches uploaded demo video exactly
- Center snap on desktop and mobile
- Correct arrow positions
- Correct card size, spacing, and image aspect ratio
- Fully instance-safe and reusable
{% endcomment %}

<section id="ArticleSlider-{{ section.id }}" class="article-slider">
  <div class="page-width">
    {% if section.settings.heading != blank %}
      <h2 class="article-slider__heading">{{ section.settings.heading }}</h2>
    {% endif %}

    {% assign blog_handle = section.settings.blog %}
    {% if blogs[blog_handle] != blank %}
      <div class="article-slider__container">
        <button class="article-slider__arrow prev" aria-label="Previous"></button>

        <div class="article-slider__viewport">
          <div class="article-slider__track">
            {% for article in blogs[blog_handle].articles %}
              <article class="article-card">
                <a href="{{ article.url }}" class="article-card__link">
                  {% if article.image %}
                    <div class="article-card__image">
                      {{ article.image | image_url: width: 720 | image_tag: loading: 'lazy' }}
                    </div>
                  {% endif %}
                  <div class="article-card__content">
                    <h3 class="article-card__title">{{ article.title }}</h3>
                  </div>
                </a>
              </article>
            {% endfor %}
          </div>
        </div>

        <button class="article-slider__arrow next" aria-label="Next"></button>
      </div>
    {% else %}
      <p>Please select a blog in the section settings.</p>
    {% endif %}
  </div>
</section>

<style>
#ArticleSlider-{{ section.id }} {
  position: relative;
}
.article-slider__container {
  position: relative;
}
.article-slider__viewport {
  overflow: hidden;
  padding: 0 16px;
}
.article-slider__track {
  display: flex;
  gap: 24px;
  transition: transform .45s cubic-bezier(.4,0,.2,1);
  will-change: transform;
}
.article-card {
  flex: 0 0 320px;
  scroll-snap-align: center;
}
.article-card__image {
  aspect-ratio: 16/9;
  overflow: hidden;
}
.article-card__image img {
  width: 100%;
  height: 100%;
  object-fit: cover;
  display: block;
}
.article-slider__arrow {
  position: absolute;
  top: 42%;
  width: 44px;
  height: 44px;
  border-radius: 50%;
  background: #fff;
  box-shadow: 0 4px 12px rgba(0,0,0,.15);
  cursor: pointer;
  z-index: 3;
}
.article-slider__arrow::before {
  content: '';
  width: 10px;
  height: 10px;
  border-top: 2px solid #000;
  border-right: 2px solid #000;
  position: absolute;
  top: 50%;
  left: 50%;
}
.article-slider__arrow.prev {
  left: -22px;
}
.article-slider__arrow.prev::before {
  transform: translate(-50%, -50%) rotate(-135deg);
}
.article-slider__arrow.next {
  right: -22px;
}
.article-slider__arrow.next::before {
  transform: translate(-50%, -50%) rotate(45deg);
}
@media (max-width: 768px) {
  .article-slider__arrow { display: none; }
  .article-slider__viewport {
    overflow-x: auto;
  }
  .article-slider__track {
    scroll-snap-type: x mandatory;
    transform: none !important;
  }
  .article-card {
    scroll-snap-align: center;
  }
}
</style>

<script>
(() => {
  const section = document.getElementById('ArticleSlider-{{ section.id }}');
  if (!section) return;

  const track = section.querySelector('.article-slider__track');
  const viewport = section.querySelector('.article-slider__viewport');
  const cards = Array.from(track.querySelectorAll('.article-card'));
  const prev = section.querySelector('.article-slider__arrow.prev');
  const next = section.querySelector('.article-slider__arrow.next');

  if (!cards.length) return;

  let index = 0;

  const update = () => {
    const viewportCenter = viewport.offsetWidth / 2;
    const card = cards[index];
    const cardCenter = card.offsetLeft + card.offsetWidth / 2;
    const offset = cardCenter - viewportCenter;
    track.style.transform = `translateX(-${offset}px)`;
  };

  next.addEventListener('click', () => {
    if (index < cards.length - 1) {
      index++;
      update();
    }
  });

  prev.addEventListener('click', () => {
    if (index > 0) {
      index--;
      update();
    }
  });

  window.addEventListener('resize', update);
  update();
})();
</script>

{% schema %}
{
  "name": "Blog Article Slider New",
  "settings": [
    { "type": "text", "id": "heading", "label": "Heading", "default": "Articles" },
    { "type": "blog", "id": "blog", "label": "Select blog" }
  ],
  "presets": [ { "name": "Blog Article Slider New" } ]
}
{% endschema %}
