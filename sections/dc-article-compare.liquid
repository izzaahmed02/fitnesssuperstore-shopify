{%  if page.metafields.custom.comparison_products.value %}

<style>

  .compareChart .scroll-arrows {
  display: flex;
  justify-content: center;
  margin: 12px 0;
  gap: 8px;
}

.compareChart .scroll-btn {
  background-color: #000;
  color: #fff;
  border: none;
  padding: 8px 16px;
  font-size: 20px;
  border-radius: 4px;
  cursor: pointer;
  z-index: 10;
}



.compareChart .scroll-btn:hover {
  background-color: #444;
}

  
  
.compareChart .col-feature {
  width: 193px;
}

.compareChart .col-product {
 width: 163.6px; 
}



@media (max-width: 768px) {
  .compareChart .col-feature {
    width: 100px;
  }

  .compareChart .col-product {
    width: 155px;
  }

  .compareChart .comparison-chart-wrapper, .compareChart .scroll-container {
    overflow-y: hidden !important;
    max-height: 100%!important;
  }

  .compareChart .custom-scrollbar.vertical {
    display: none !important;
  }

  

}

@media (max-width: 500px) {
  .compareChart .col-feature {
    width: 80px;
  }

  .compareChart .col-product {
    width: 140px;
  }

}



.compareChart .scroll-container {
  position: relative;
  width: 100%;
  max-height: 100vh;
}

.compareChart .comparison-chart-wrapper {
  overflow: scroll;
  max-height: 100vh;
  height: 100%;
  width: 100%;
  scrollbar-width: none;          
  -ms-overflow-style: none;      
  border-radius: 12px;
}

.compareChart .custom-scrollbar {
  position: absolute;
  background: #999;
  opacity: 1;
  z-index: 20;
  pointer-events: none;
}

.compareChart .custom-scrollbar.horizontal {
  height: 5px;
  top: 0;
  left: 0;
  border-radius: 4px;
  background: #aaa;
  z-index: 1;
}

.compareChart .custom-scrollbar.vertical {
  width: 5px;
  top: 0;
  right: 0;
  border-radius: 4px;
  background: #aaa;
  z-index: 1;
}

  .compareChart .scrollbar-container {
  position: relative;
}

.compareChart .comparison-chart-wrapper::-webkit-scrollbar {
  display: none;
}
.compareChart .comparison-chart-wrapper {
  -ms-overflow-style: none;  
  scrollbar-width: none;  
}

.compareChart .comparison-chart-outer{
    padding: 0px 16px;
  }

  .compareChart .feature-col{

    max-width: 100%;
      font-weight: 600;
  text-align: left;
  position: sticky;
    left: 0;
    z-index: 1;
  }



  .compareChart thead, .compareChart .even-class{
    background: #EDEFF3;

  }
.compareChart .comparison-chart-outer {
  width: 100%;
  overflow-y: visible;
  position: relative;
  margin-bottom: 50px;
}

.compareChart .comparison-table {
  width: max-content;
  border-collapse: collapse;
  table-layout: fixed;
}

.compareChart .comparison-table th{
  padding: 12px;
  border: 1px solid #e5e5e5;
  text-align: center;
  vertical-align: top;
  word-wrap: break-word;
  white-space: normal;
}

.compareChart .comparison-table td {
  padding: 12px;
  border: 1px solid #e5e5e5;
  text-align: left;
  vertical-align: top;
  word-wrap: break-word;
  white-space: normal;
}


   .compareChart .comparison-table thead th.feature-col {
    z-index: 1;
     background: #EDEFF3;
  }

.compareChart .feature-label {
  text-align: left;
color: #000;
  font-family: Lato;
font-weight: 700;
font-style: Bold;
font-size: 16px;
line-height: 24px;
letter-spacing: 0;
vertical-align: middle;
    position: sticky;
    left: 0;
    z-index: 1;
}

  .compareChart .even-class .feature-label {
       background-color: #EDEFF3;
  }

  .compareChart .odd-class .feature-label {
    background-color: #fff;
  }

.compareChart .feature-value {
  color: #000;
  font-family: Lato;
  font-weight: 400;
  font-style: Regular;
  font-size: 16px;
  line-height: 24px;
  letter-spacing: 0px;
  vertical-align: middle;
}

.compareChart .product-header {
  text-align: center;
}

.compareChart .product-title {
  font-family: Lato;
font-weight: 700;
font-style: Bold;
font-size: 18px;
line-height: 24px;
letter-spacing: 0px;
text-align: center;
  color: #000;
  margin-bottom: 24px;

}

.compareChart .product-header-image img {
  max-width: 100px;
  width: 91px;
    height: 120px;

}

  .compareChart .customTitle{
font-family: Oswald;
font-weight: 600;
font-size: 48px;
margin-bottom: 52px;
line-height: 68px;
letter-spacing: 0;
text-align: center;
text-transform: uppercase;
    

  }

  .compareChart .comparisonDescp{
    max-width: 842px;
    margin: 0 auto;
  }

  .compareChart .customContent{
font-family: Lato;
font-weight: 400;
font-size: 16px;
line-height: 24px;
letter-spacing: 0px;
text-align: center;

  }

.compareChart .right_arrow svg {
  width: 20px;
  animation: move 1s linear infinite;
  transform-origin: center;
}

  .compareChart .right_arrow{
    text-align: right;
    margin-right: 10px;
  }

@keyframes move {
  0% {
    transform: translateX(0);
  }
  50% {
    transform: translateX(5px);
  }
  100% {
    transform: translateX(0);
  }
}



@media (max-width: 768px) {

   .compareChart .customTitle{
font-family: Oswald;
font-weight: 600;
font-size: 24px;
margin-bottom: 20px;
line-height: 32px;
letter-spacing: 0;
text-align: center;
text-transform: uppercase;
    

  }

      .compareChart .comparisonDescp {
        padding: 0 16px;
    }
  .compareChart .product-header-image img {
    max-width: 80px;
  }

}

</style>

  

<div class="comparisonDescp">
<h1 class="main-page-title page-title customTitle{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--fade-in{% endif %}">
    {{ page.title | escape }}
  </h1>
  <div class="customContent">{{ page.content }}</div>
  </div>






<div class="comparison-chart-outer compareChart">
  <div class="scroll-arrows">
    <button class="scroll-btn left" aria-label="Scroll left">&larr;</button>
    <button class="scroll-btn right" aria-label="Scroll right">&rarr;</button>
  </div>

  <div class="scroll-container">
    <div class="comparison-chart-wrapper" id="scrollableContent">
             <table class="comparison-table">
  <colgroup>
  <col class="col-feature">
  {% assign products = page.metafields.custom.comparison_products.value %}
  {% for product in products %}
    <col class="col-product">
  {% endfor %}
</colgroup>

      <thead>
        <tr>
          <th class="feature-col"></th>
          {% assign products = page.metafields.custom.comparison_products.value %}
          {% assign feature_names = '' %}
          {% assign product_features_map = '' %}
          {% for product in products %}
            {% assign feature_map = '' %}
            {% for feature in product.features.value %}
              {% assign fname = feature.name %}
              {% assign fvalue = feature.value %}
              {% unless feature_names contains fname %}
                {% assign feature_names = feature_names | append: fname | append: '||' %}
              {% endunless %}
              {% assign feature_map = feature_map | append: fname | append: '::' | append: fvalue | append: '||' %}
            {% endfor %}
            {% assign product_features_map = product_features_map | append: feature_map | append: '##' %}
          {% endfor %}
          {% assign feature_names = feature_names | split: '||' | uniq %}
          {% assign product_feature_blocks = product_features_map | split: '##' | reject: '' %}

          {% for product in products %}          
            <th class="product-header">
               <a href="{{ product.url }}">
              <div class="product-title">{{ product.custom_title }}</div>
              {% if product.image %}
                <div class="product-header-image">
                  <img
                    src="{{ product.image | image_url: width: 200 }}"
                    alt="{{ product.custom_title | escape }}"
                    loading="lazy"
                  >
                </div>
              {% endif %}
               </a>
            </th>           
          {% endfor %}
        </tr>
      </thead>
     <tbody>
  {% for fname in feature_names %}
    {% unless fname == blank %}
      {% assign mod = forloop.index | modulo: 2 %}
      <tr class="{% if mod == 0 %}even-class{% else %}odd-class{% endif %}">
        <td class="feature-label">{{ fname }}</td>
        {% for product in products %}
          {% assign match = '—' %}
          {% for feature in product.features.value %}
            {% if feature.name == fname %}
              {% assign match = feature.value %}
              {% break %}
            {% endif %}
          {% endfor %}
          <td class="feature-value">{{ match }}</td>
        {% endfor %}
      </tr>
    {% endunless %}
  {% endfor %}
</tbody>

    </table>
    </div>
  </div>
</div>



<script>
document.addEventListener("DOMContentLoaded", function () {
  const scrollableContent = document.getElementById("scrollableContent");

  const leftBtn = document.querySelector(".scroll-btn.left");
  const rightBtn = document.querySelector(".scroll-btn.right");

  if (scrollableContent && leftBtn && rightBtn) {
    leftBtn.addEventListener("click", function () {
      scrollableContent.scrollBy({ left: -200, behavior: 'smooth' });
    });

    rightBtn.addEventListener("click", function () {
      scrollableContent.scrollBy({ left: 200, behavior: 'smooth' });
    });
  } else {
    console.error("Scroll buttons or scrollable content not found.");
  }
});
</script>






{% else %}


{{ 'main-article-compare.css' | asset_url | stylesheet_tag }}


<div class="page-width">
    <div class="comparison-main__wrapper">
        <h1 class="comparison-main__title">
            {{ page.title }}
        </h1>
        {{ page.content }}
    </div>
</div>


<script>
	document.addEventListener('DOMContentLoaded', () => {
		const scrollArea = document.querySelector('.comparison-scroll-area');
		const btnLeft = document.querySelector('.scroll-btn--left');
		const btnRight = document.querySelector('.scroll-btn--right');
		const tableHeaderRow = document.querySelector('.comparison-table thead tr');
		const columns = tableHeaderRow.querySelectorAll('th:not(.sticky-col)');
		const colCount = columns.length;
		if (colCount <= 4) {
			btnLeft.style.display = 'none';
			btnRight.style.display = 'none';
			return;
		}
		const firstCol = columns[0];
		let scrollStep = firstCol.offsetWidth;

		function updateArrows() {
			if (scrollArea.scrollLeft <= 0) {
				btnLeft.classList.add('disabled');
			} else {
				btnLeft.classList.remove('disabled');
			}
			const atRightEdge = scrollArea.scrollLeft + scrollArea.clientWidth >= scrollArea.scrollWidth - 1;
			if (atRightEdge) {
				btnRight.classList.add('disabled');
			} else {
				btnRight.classList.remove('disabled');
			}
		}

		updateArrows();
		scrollArea.addEventListener('scroll', updateArrows);
		btnLeft.addEventListener('click', () => {
			if (btnLeft.classList.contains('disabled')) return;
			scrollArea.scrollBy({
				left: -scrollStep,
				behavior: 'smooth'
			});
		});
		btnRight.addEventListener('click', () => {
			if (btnRight.classList.contains('disabled')) return;
			scrollArea.scrollBy({
				left: scrollStep,
				behavior: 'smooth'
			});
		});
	});
</script>

  {% endif %}


{% schema %}
{
  "name": "Comparison Inner",
  "class": "section comparison-main compareChart",
  "tag": "section",
  "disabled_on": {
    "groups": [
      "header",
      "footer"
    ]
  },
  "settings": [
    {
      "type": "color",
      "id": "bg",
      "label": "Background color",
      "default": "#fff"
    },
    {
      "type": "text",
      "id": "title",
      "label": "Title"
    },
    {
      "type": "text",
      "id": "subtitle",
      "label": "SubTitle"
    },
    {
      "type": "header",
      "content": "t:sections.all.padding.section_padding_heading"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_top",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_bottom",
      "default": 36
    },
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "image_picker",
      "id": "image_left",
      "label": "Left Image"
    }
  ],
  "blocks": [
    {
      "type": "item-num",
      "name": "item with nums and bulls",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Title"
        },
        {
          "type": "richtext",
          "id": "subtitle",
          "label": "Subtitle"
        },
        {
          "type": "richtext",
          "id": "text",
          "label": "Text"
        }
      ]
    }
  ]
}
{% endschema %}
