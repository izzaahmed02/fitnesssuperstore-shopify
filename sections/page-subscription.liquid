{{ 'homepage-subscription.css' | asset_url | stylesheet_tag }}

<div class="page-width">
    <div class="homepage-subscription__wrapper {% if template.name == 'page' %}
page-subscription__wrapper
    {% endif %}">
        <h2 class="homepage-subscription__title">{{ section.settings.title }}</h2>
        <div class="homepage-subscription__content">
            <form action="#" class="homepage-subscription__form">
                <label for="email" class="homepage-subscription__from-label hidden"></label>
                <input placeholder="Enter your email" type="email" id="email" class="homepage-subscription__input">
                <button class="homepage-subscription__button" type="submit">Subscribe
                    {% render 'chevron-button' %}
                </button>
            </form>
            <span class="homepage-subscription__form-copy">
        {% render 'form-checkmark' %}
                {{ section.settings.label }}
        </span>
            <div class="homepage-subscription__original-form">
                <div class="klaviyo-form-Ud8shK"></div>
            </div>
        </div>
    </div>
    <div class="homepage-subscription__message">
        <h2 class="homepage-subscription__message-title">{{ section.settings.thank-you }}</h2>
        <p class="homepage-subscription__message-copy">{{ section.settings.thank-you-copy }}</p>
    </div>
</div>

<script src="{{ 'homepage-subscription.js' | asset_url }}" defer="defer"></script>

{% schema %}
{
  "name": "Homepage Subscription",
  "tag": "section",
  "class": "section homepage-subscription",
  "disabled_on": {
    "groups": [
      "header",
      "footer"
    ]
  },
  "settings": [
    {
      "type": "text",
      "id": "title",
      "label": "Section Title"
    },
    {
      "type": "text",
      "id": "label",
      "label": "Section  Label"
    },
    {
      "type": "text",
      "id": "thank-you",
      "default": "Thanks for subscribing!",
      "label": "Section  Message Title"
    },
    {
      "type": "text",
      "id": "thank-you-copy",
      "default": "Check your email for a confirmation message.",
      "label": "Section  Message Copy"
    }
  ],
  "presets": [
    {
      "name": "Homepage Subscription"
    }
  ]
}
{% endschema %}
